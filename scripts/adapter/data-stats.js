/* scripts/adapter/data-stats.js */

/**
 * Retorna dados da aba de stats (placeholder — stats são exibidos diretamente via template).
 * @param {Actor} actor
 * @returns {{ items: Array }}
 */
export function getStatsData(actor) {
    return { items: [] };
}
